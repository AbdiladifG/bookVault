<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Book Search</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 20px;
        background-color: #f0f0f0;
      }
      .container {
        max-width: 1200px;
        margin: 0 auto;
      }
      h1 {
        text-align: center;
        color: #333;
      }
      .search-container {
        display: flex;
        justify-content: center;
        margin-bottom: 20px;
      }
      #search-input {
        width: 70%;
        max-width: 400px;
        padding: 10px;
        font-size: 16px;
        border: 1px solid #ddd;
        border-radius: 4px 0 0 4px;
      }
      #search-button {
        padding: 10px 20px;
        font-size: 16px;
        background-color: #4caf50;
        color: white;
        border: none;
        border-radius: 0 4px 4px 0;
        cursor: pointer;
      }
      .results {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
        gap: 20px;
        justify-content: center;
        max-width: 900px;
        margin: 0 auto;
      }
      .book {
        background-color: white;
        border-radius: 4px;
        padding: 15px;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      }
      .book img {
        width: 100%;
        height: 200px;
        object-fit: cover;
        border-radius: 4px;
        margin-bottom: 10px;
      }
      .book h3 {
        margin: 0 0 5px 0;
        font-size: 18px;
      }
      .book p {
        margin: 0 0 10px 0;
        color: #666;
      }
      .bookmark-btn {
        background-color: #008cba;
        color: white;
        border: none;
        padding: 5px 10px;
        border-radius: 4px;
        cursor: pointer;
      }
      .delete-btn {
        background-color: rgba(200,0,0);
        color: white;
        border: none;
        padding: 5px 10px;
        border-radius: 4px;
        cursor: pointer;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Book Search</h1>
      <div class="search-container">
        <input
          type="text"
          id="search-input"
          placeholder="Enter book title or author"
        />
        <button id="search-button">Search</button>
      </div>
      <div class="results"></div>
    </div>

    <div class="container">
      <h1>Bookmarked Books</h1>
      <div class="results">
        <%for(var i=0; i < books.length; i++){%>
          <div class="book">
           <img src="<%=books[i].imageLink%>" alt="Book cover" />
           <h3><%=books[i].title%></h3>
           <p><%=books[i].author%></p>
           <button class="delete-btn"  id="<%=books[i]._id%>" type="submit">Delete</button>
           
         </div> <%}%>
      </div>
    </div>
    
    <script src="main.js"></script>
  </body>
</html>
